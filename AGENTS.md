# AGENTS.md

문서 버전: 0.11.0 · 최종 수정일: 2026-09-08 · 역할: 상시 실행 규칙

## 1. 시작: 무엇을 읽고 무엇을 믿는가

**목표: Main의 학습 목표를 이어가면서, 숫자 치환이 아닌 새로운 풀이 경험을 제공한다.**

이 파일은 Astra High 지휘관이 빠르게 재개하도록 구성했으며 사람·Sol·Terra도 같은 규칙을 읽는다. 모델 전용 숨은 형식이나 두 번째 규칙 사본은 없다. 실제 모델 설정을 이 파일이 바꾸지는 않는다.

1. 최신 사용자 요청에서 이번 작업의 목표·변경 허용 범위·중단 조건을 확인한다.
2. [현재 상태](docs/PROJECT_STATUS.md)의 **다음 작업·알려진 차이**를 읽는다.
3. [제품 명세](docs/SPEC.md)의 §0 읽기 지도에서 관련 FR·QG·AC와 본문 절을 고른다.
4. 해당 코드·테스트·산출물로 실제 상태를 확인한다. 명세에 쓰였다는 이유로 구현 완료로 간주하지 않는다.
5. 허용된 변경과 완료 증거를 한 문장으로 정한 뒤 수행한다.

이 문서는 시스템·개발자 지시를 대체하지 않는다. 그 경계 안에서는 최신 사용자 결정이 저장소 문서보다 우선한다. 제품 계약은 `docs/SPEC.md`, 실행 규칙은 이 파일, 변하는 진행 사실은 `docs/PROJECT_STATUS.md`가 원문이다. 루트 `SPEC.md`는 안내 링크다.

## 2. 지휘관의 작업 루프

메인 개발에서 root는 **지휘·검토 전담**이다. 코드·문서 편집은 하위 에이전트에 위임하고 root는 방향·수용 기준·결과 검토와 필요한 읽기 전용 검사를 맡는다.

`범위/AC 선택 → 담당·파일 소유권 지정 → 실행 → 산출물/실패 검토 → 수정 지시 → 증거 확인 → 상태 갱신`

- 매 단계 종료를 사용자 승인 지점으로 만들지 않는다. 이미 허가된 같은 목표의 다음 작업을 이어간다.
- 중단은 큰 완료 단위 달성, 필수 사용자 결정, 새 외부 권한·비용, 또는 안전한 대안까지 확인한 실제 실행 장애일 때 한다.
- 목표·품질 기준 변경이나 외부 공개는 임의로 수행하지 않는다.
- 새 정보가 없으면 같은 검사·파일 읽기를 반복하지 않는다. 변경 위험에 맞는 검사가 통과하면 다음 일로 간다.
- 보고는 결과·근거·남은 한계 순서로 간결하게 쓴다. 실패를 숨기거나 부분 결과를 완제품처럼 표현하지 않는다.

## 3. 위임 계약

| 역할 | 최소 모델 기준 | 입력·책임 |
|---|---|---|
| 생성/고난도 분석/수학 검수 | `gpt-5.6-sol` 이상 | 명세와 해당 단계 packet; 실제 계산·조건 검토 |
| 문서 담당 | `gpt-5.6-terra` 이상 | 확인된 근거·결정·파일 범위; 상태 동기화 |
| blind solver | `gpt-5.6-sol` 이상, 새 컨텍스트 | 문제·선택지·답 형식만 받고 독립 풀이 |

실제 사용한 모델 ID·역할·reasoning 설정·입력 가시성을 기록한다. 파일 내용으로 모델을 사용했다고 추정하지 않는다. root 포함 동시 4명 이하, 하위 3명 이하로 운용한다. 문서 담당은 필요할 때만 배정한다. 병렬 작업은 서로 다른 파일 또는 명확한 소유권으로 분리한다.

위임 시 다음 항목을 모두 전달한다.

```text
목표 / 관련 FR·QG·AC:
읽어도 되는 입력 / 보면 안 되는 입력:
수정 가능한 파일 / 다른 담당 소유 파일:
수용 기준 / 실행할 검사:
실패 시 처리 / 보존할 산출물:
최종 보고: 변경 파일, 실제 검사 결과, 남은 위험, 다음 작업
```

도구·현재 대화의 지시가 위임을 금지하거나 모델 실행이 막히면 이를 우회하지 않는다. 사용할 수 없는 팀·모델로 작업했다고 기록하지 않는다.

## 4. 제품 불변 규칙

- Main과 무관한 새 문제도 실패다. 보존한 학습 목표와 새 추론을 구체적으로 설명한다.
- 숫자·계수·기호·이름만 바꾼 문제는 실패다. `changed_elements` 목록 존재만으로 신규성을 승인하지 않는다.
- `role`과 `difficulty_delta`는 별개다. Sketch는 병목을 겨냥해 쉬워지고, Addition은 Main과 비슷하거나 조금 더 어려워야 한다.
- 여러 후보가 서로 다른 상품이라는 주장은 후보 간 의미 중복 검사 전에는 하지 않는다.
- JSON은 주 데이터, PDF는 관계·구조·시각 근거다. donor에 없는 가족 역할을 만들어 넣지 않는다.
- `family_role`과 원문 출처·저자는 별개다. PDF 배치 관계를 저자 생성 계보로 단정하지 않는다.
- 같은 가족·중복 그룹의 평가 누수를 막는다. development에서 조정하고 validation 뒤 동결 test를 수행한다.
- 전체 입력은 현재 확보 자료다. 추가 자료·외부 인원·사용자의 수학 검토를 개발 선행 조건으로 요구하지 않는다.
- 인터뷰는 개발 로드맵에 넣지 않는다. 기하·벡터·자동 작도는 최후순위이며 배포 이전 연구는 별도 요청 전에는 시작하지 않는다.

## 5. 생성·검증·승인 경계

기본 순서: `normalize → analyze → retrieve → plan → generate → blind solve → verify/audit → rank → status`.
실험의 baseline은 계획 없는 비교군임을 명시한다.

- 생성자는 평가 가족의 파생 문항·정답·test reference를 보지 않는다.
- blind solver에게 생성 정답·해설·Blueprint·Plan·난이도 자기평가·역할 힌트를 주지 않는다. 현재 payload는 `problem_text`, `answer_type`, `choices`만 허용한다.
- 독립 풀이를 동결한 뒤 별도 감사자가 원문·후보·독립 풀이를 비교한다. 생성자에게 감사 역할까지 맡기지 않는다.
- fresh context는 입력 제한의 운용 방법이지 파일시스템 격리나 오류 독립성의 증명이 아니다. 실제 공유 맥락이 있으면 기록한다.
- 답 불일치, warnings, 낮은 confidence, 검증 불능은 자동 승인하지 않는다. 가능한 결정적 검산과 근거 검토를 병행한다.
- unresolved 후보는 최대 두 번 수정·재생성한 뒤 reject/park한다. 첫 시도·실패·수정 이력은 모두 보존한다.
- `retain`은 검수자의 유지 추천일 뿐이다. `needs_review`는 판매 가능 결과가 아니다.
- `technical_status=accepted`와 `publication_status=approved`를 구분한다. 판매 승인은 기술 검증과 별도 운영자 승인 뒤에만 가능하다.
- 현 runtime의 상태와 `verification.passed` 의미 차이는 [알려진 차이](docs/PROJECT_STATUS.md#4-알려진-차이와-미검증-범위)를 따른다. 미평가 항목을 문서에서 통과로 바꾸지 않는다.

## 6. 구현·데이터 경계

- Python 3.11+, Pydantic 입력 검증, `LLMProvider` 호출 경계를 유지한다.
- 기존 코드·표준 라이브러리를 먼저 사용한다. 미요청 프레임워크·별도 벡터 DB·추상화를 추가하지 않는다.
- 결정적 테스트에 실제 모델 호출을 섞지 않는다. unittest·fixture replay·실제 생성 평가를 별도로 보고한다.
- 프롬프트/모델/데이터/검증기 버전과 hash-bound 원시 응답을 남긴다. 불명확한 token·cost·추론 지연시간은 `null`이다.
- 원본 PDF·JSON과 기존 사용자 변경을 보존한다. raw/intermediate/normalized를 분리하고 파싱 실패를 기록한다.
- 비공개 원본·풀이·키·로그는 공개 커밋하지 않는다. `data/raw/`, `artifacts/private/`, PDF·비밀 파일은 Git 제외 상태를 확인한다.
- 비로컬 endpoint로 원문·donor를 전송하려면 명시적 허가가 필요하다.
- 교수자 지정 경로는 README의 제출 구조를 따른다. 현재 `mathgen/` 코드를 `src/`로 이동하는 작업은 별도 구현 변경이며 문서 정리와 섞지 않는다.

## 7. 완료 증거와 문서 관리

- 관련 AC를 검사하고 성공·실패 결과를 남긴다. 테스트 통과는 수학·교육 품질 통과와 다르다.
- 코드 수정 시 관련 검사를 실행한다. 문서만 수정했다면 링크·경로·ID·사실 일치 검사를 우선하고 불필요한 모델 평가를 돌리지 않는다.
- 진행 수치·다음 일·미검증 범위는 `docs/PROJECT_STATUS.md`에 갱신한다. AGENTS.md에 실험 개수를 반복 복사하지 않는다.
- 제품 변경은 `docs/SPEC.md`의 버전·결정·이력과 AC를 함께 갱신한다. README는 진입점·제출 구조·실행 요약만 유지한다.
- 기존 FR/QG/DEC/AC 번호를 재사용·삭제하지 않는다. 변경 사유와 evidence 연결을 남긴다.
- 수업용 인터뷰, 관찰, 사람 승인, 골든 케이스를 날조하지 않는다. AI 초안과 사람 확인 완료를 구분한다.
- 제출용 산출물의 선행 조건이 없으면 `pending`으로 기록한다. 파일·폴더 존재만으로 산출물 완료를 표시하지 않는다.

## 8. 최소 용어집

| 대표어 | 의미 |
|---|---|
| Main | 변형 출발점인 원문 |
| Sketch / Addition | 병목 연습 / 구조 확장 관계; 난이도 필드와 별개 |
| ProblemBlueprint | 개념·조건·목표·풀이·병목의 구조화 분석 |
| VariationPlan | 보존·변경·금지 요소와 예상 풀이 설계 |
| problem family | Main과 관련 Sketch·Addition의 평가 분할 단위 |
| donor | 검색·중복 검사용 문항; 자동으로 가족 관계를 부여하지 않음 |
| blind solve | 생성 답·해설을 보지 않은 독립 풀이 |
| audit / retain | 근거를 갖춘 검수 / 유지 추천; 기술·판매 승인과 별개 |

상세 정의와 실제 필드명은 `docs/SPEC.md` §5·9~10, `mathgen/models.py`를 따른다.
