# AGENTS.md

| 항목 | 값 |
|---|---|
| 문서 상태 | Living Guide |
| 문서 버전 | 0.7.0 |
| 최종 수정일 | 2026-09-04 |
| 제품 명세 | `SPEC.md` |
| 프로젝트 안내 | `README.md` |

이 문서는 AI 수학 문제 변형 생성기 저장소에서 작업하는 개발자와 AI 에이전트의 실행 규칙만 정의한다. 제품 요구사항과 품질 기준의 원문은 `SPEC.md`를 따른다.

## 1. 작업 시작 순서

다음 순서로 현재 작업의 근거를 확인한다.

1. 사용자의 최신 직접 지시
2. `SPEC.md`의 관련 요구사항과 결정
3. 이 문서의 작업 규칙
4. `README.md`의 현재 상태와 실행 방법
5. 실제 코드, 테스트와 데이터 상태

사용자 결정과 문서가 충돌하면 사용자 결정을 따른다. 변경 작업을 요청받았다면 같은 작업에서 관련 문서를 갱신한다. 검토·설명만 요청받았다면 파일을 수정하지 않고 불일치만 보고한다.

작업 전에는 다음을 확인한다.

- 관련 요구사항 ID(`FR-*`, `QG-*`, `DEC-*`)
- 실제 파일과 테스트 상태
- 입력, 출력과 실패 조건
- 원본 데이터와 사용자 변경사항의 보존 여부
- 요청을 충족하는 가장 작은 구현 단위

## 2. 제품 불변 원칙

- Main의 학습 목표를 이어가면서 새로운 풀이 경험을 제공한다.
- 숫자·계수·기호·이름만 바꾼 문제는 실패로 처리한다.
- `role`과 `difficulty_delta`를 별도 필드로 관리한다.
- PDF의 가족 역할과 원문 출처·저자 표기를 별도 필드로 관리한다.
- JSON을 주 데이터로 사용하고 PDF는 구조·시각 검증에 사용한다.
- 같은 문제 가족을 학습·검증·테스트에 나누어 넣지 않는다.

기술 검증을 통과한 결과와 실제 판매 승인을 구분한다. `technical_status=accepted`만으로 `publication_status=approved`가 되지 않는다.

## 3. 현재 범위

- 현재 단계는 데이터 기반, 모델 독립 전체 생성 파이프라인, 결정적 검증기와 로컬 브라우저 데모 구현 완료다.
- 현재 확보한 자료를 전체 입력으로 보고 MVP 개발을 진행한다. 추가 데이터가 들어오면 같은 진단기로 확장한다.
- PDF에서 명시적으로 확인한 41개 가족·205문항은 관계 reference로 사용하고, donor 499문항은 검색·중복 탐지 corpus로 사용한다.
- MVP는 공통·미적분 23개 가족으로 시작한다. 3회는 development, 5회는 validation, 4회는 test이며 확률과 통계·기하는 parked다.
- 스키마만 통과한 하네스 결과는 `needs_review`이고 미구현 게이트를 명시한다. 독립 풀이와 Hard Gate 전에는 `accepted`로 올리지 않는다.
- localhost가 아닌 모델 endpoint로 원문·donor를 보내려면 사용자가 데이터 외부 전송을 명시적으로 허용해야 한다.
- 최종 생성 모델은 평가 하네스를 만든 뒤 결정한다.
- 인터뷰는 개발 로드맵에 포함하지 않는다.
- 기하·벡터·자동 작도는 최후순위로 유지한다.

문서에 적힌 디렉터리, API와 모델은 목표 또는 초안일 수 있다. 실제 파일이 없으면 구현된 것으로 간주하지 않는다.

## 4. SDD 작업 방식

### 구현 전

1. 해결할 문제와 완료 결과를 한 문장으로 적는다.
2. 관련 요구사항 ID를 선택한다.
3. 입력, 출력과 실패 상태를 확인한다.
4. 수용 기준을 테스트 가능한 문장으로 만든다.
5. 가장 작은 수직 기능 단위를 선택한다.

요구사항이 없으면 구현 전에 `SPEC.md`에 요구사항과 완료 기준을 추가한다.

### 구현 중

- 기존 코드와 표준 라이브러리를 먼저 사용한다.
- 모델 출력은 신뢰하지 않고 스키마와 검증기를 통과시킨다.
- 프롬프트, 모델, 데이터와 검증기 버전을 기록한다.
- 실패를 숨기거나 버리지 않고 코드와 근거를 저장한다.
- 현재 요구사항에 없는 추상화와 확장 기능을 추가하지 않는다.

### 구현 후

- 관련 테스트와 수용 기준을 실행한다.
- 실제 실행 결과를 완료 증거로 남긴다.
- 허가된 변경 범위 안에서 관련 문서를 동기화한다.
- 남은 위험과 검증하지 못한 범위를 기록한다.
- 데모가 한 번 성공한 것만으로 완료 처리하지 않는다.

## 5. 데이터와 공개 규칙

- PDF와 JSON 원본을 직접 수정하지 않는다.
- 원본, 중간 결과와 정규화 결과를 분리한다.
- 원시 값과 정규화 값을 함께 보존하고 파싱 실패를 기록한다.
- 안정적인 `source_problem_id`와 `family_id`를 사용한다.
- 권리가 확인되지 않은 원본, API 키, 모델 가중치와 민감 로그를 공개 커밋하지 않는다.

공개 저장소에는 허가받았거나 직접 만든 최소 샘플만 둔다. `data/raw/`, PDF, 비밀 파일과 민감 로그는 `.gitignore`에 포함한다.

## 6. 구현 경계

- 주 개발 언어는 Python 3.11 이상이다.
- 입력 경계와 모델 구조화 출력은 Pydantic으로 검증한다.
- 모델 호출은 `LLMProvider` 경계 안에 둔다.
- 결정적으로 테스트할 수 있는 코드에 LLM 호출을 섞지 않는다.
- 초기에는 대형 에이전트 프레임워크와 별도 벡터 DB를 도입하지 않는다.

약 5,000문항 규모에서는 단순한 로컬 검색부터 시작한다. 새 의존성, 서비스 또는 추상화는 현재 요구사항에 필요한 근거가 있을 때만 추가한다.

## 7. 생성과 검증

기본 처리 순서는 다음과 같다.

```text
normalize → analyze → retrieve → plan → generate
→ solve independently → verify → rank → accept or reject
```

- 생성 전에 보존 요소와 변경 요소가 있는 Variation Plan을 만든다.
- Sketch는 Main의 특정 병목을, Addition은 의미 있는 구조 변경을 근거로 남긴다.
- 생성 답과 분리된 독립 풀이 및 가능한 결정적 검산을 수행한다.
- 중복, 숫자 치환과 기존 문제 근사 복제를 검사한다.
- 검증 불능 또는 불일치 결과를 자동 승인하지 않는다.

`needs_review`는 판매 가능한 결과가 아니다. `publication_status=approved`는 기술 검증과 별도의 운영자 승인 후에만 설정한다.

## 8. 테스트 원칙

최소 테스트 범위는 다음과 같다.

- canonical schema와 JSON 로더
- 문제 가족 분할 및 평가 누수
- 구조화 출력 파서와 provider 계약
- 수식 검산, 숫자 치환과 중복 탐지
- 고정 평가 세트 기반 회귀 실행

결정적 단위 테스트, 저장된 모델 응답 fixture와 실제 모델 평가를 분리한다. 테스트 이름이나 설명은 관련 `FR-*` 또는 `QG-*`를 추적할 수 있어야 한다.

## 9. 문서 책임

| 문서 | 기록할 내용 |
|---|---|
| `SPEC.md` | 제품 범위, 요구사항, 계약, 품질 게이트, 결정과 미결 사항 |
| `AGENTS.md` | 작업 방식, 구현 경계, 데이터·검증 규칙 |
| `README.md` | 프로젝트 소개, 기술 스택, 실행 방법과 전체 로드맵 |
| `SOLO_DEVELOPMENT_ROADMAP.md` | 개인 개발의 세부 실행 순서와 산출물 |

문서를 중복 복사하지 않는다. 사실이나 결정이 바뀌면 해당 문서의 버전·날짜를 갱신하고 `SPEC.md` 변경 이력에 핵심 변경을 기록한다. 완료하지 않은 작업과 측정하지 않은 수치를 사실처럼 쓰지 않는다.

## 10. JSON 수령 시 첫 작업

1. 파일 목록, 크기, 인코딩, 해시와 JSON 문법을 검사한다.
2. 전체 필드, 자료형, 누락, 중복과 실패 레코드를 집계한다.
3. 문제·보기·답·해설·수식·이미지 의존 문항을 표본 확인한다.
4. PDF 자료 및 Main·Sketch·Addition 관계와 매칭한다.
5. 결과를 근거로 canonical schema, 미결 사항과 첫 구현 작업을 갱신한다.

실제 JSON을 확인하기 전에 필드명을 추정해 본 파서를 구현하지 않는다.

## 11. 완료와 인수인계

완료 조건은 다음과 같다.

- 관련 요구사항과 수용 기준을 충족한다.
- 관련 테스트가 통과한다.
- 실패 경로와 데이터 보존을 확인한다.
- 필요한 문서와 진행 상태가 실제 결과와 일치한다.
- 남은 위험과 미검증 범위가 명시되어 있다.

다른 작업자에게 넘길 때는 아래 형식을 사용한다.

```text
목표:
관련 요구사항:
완료한 작업:
변경 파일:
실행한 테스트와 결과:
남은 위험:
다음 작업:
```
