# AI 수학 문제 변형 생성기 - Product & Software Specification

| 항목 | 값 |
|---|---|
| 문서 상태 | Draft / Living Specification |
| 명세 버전 | 0.7.1 |
| 최종 수정일 | 2026-09-04 |
| 개발 형태 | 개인 프로젝트 |
| 현재 단계 | 모델 독립 전체 파이프라인·결정적 검증·로컬 브라우저 데모 구현 완료 |
| 제품 명세의 기준 문서 | `SPEC.md` |
| 에이전트 작업 규칙 | `AGENTS.md` |
| 프로젝트 안내 및 로드맵 | `README.md` |

## 1. 문서 목적

이 문서는 AI 수학 문제 변형 생성기의 제품 목표, 범위, 데이터 계약, 기능 요구사항, 품질 기준과 완료 조건을 정의한다. 구현 방식이나 사용 모델이 바뀌더라도 제품이 해결해야 하는 문제와 품질 기준은 유지한다.

이 문서는 고정된 최종 문서가 아니라 사용자와의 대화 및 데이터 분석 결과에 따라 갱신되는 살아 있는 명세이다. 코드가 명세와 다르면 다음 순서로 처리한다.

1. 최신 사용자 결정이 무엇인지 확인한다.
2. 사용자 결정에 맞게 이 명세를 먼저 수정한다.
3. 변경된 요구사항 ID를 기준으로 코드와 테스트를 수정한다.
4. 변경 이력과 아직 해결하지 못한 위험을 기록한다.

## 2. 제품 배경

이 프로젝트는 수험생에게 판매할 수 있는 새로운 수학 문제 콘텐츠를 제작하기 위해 시작되었다.

수험생은 비용을 지불하고 구매한 문제에서 기존 문항의 숫자, 계수, 함수 이름 또는 보기만 달라진 문제를 기대하지 않는다. 그러한 문항은 새로운 사고를 연습하게 하지 못하며, 구매자가 느끼는 학습 가치와 상품 가치도 낮다.

이 제품은 원문 문제의 학습 목표와 핵심 아이디어를 보존하면서도 조건, 표현, 보조 개념 또는 풀이 경로를 의미 있게 바꾸어 사용자가 실제로 새로운 문제를 풀었다고 느끼게 하는 것을 목표로 한다.

## 3. 최상위 제품 원칙

### P-001. 새로운 풀이 경험이 최우선이다

생성 문제는 표면적으로만 다른 문제가 아니라 원문과 구별되는 새로운 조건 해석 또는 추론을 요구해야 한다.

```text
같은 숫자 구조 + 다른 숫자 ≠ 새로운 문제
같은 학습 목표 + 새로운 추론 경험 = 목표 문제
```

### P-002. 학습 목표의 연결을 유지한다

새롭기만 하고 Main과 무관한 문제는 실패다. 모든 생성 결과는 Main의 어떤 개념, 병목 또는 풀이 원리를 보존했는지 설명할 수 있어야 한다.

### P-003. 수학적 유효성은 판매 가능성의 선행 조건이다

문제가 풀리지 않거나, 조건이 모순되거나, 정답이 잘못되었거나, 해설이 정답으로 이어지지 않으면 새로운 문제라도 사용할 수 없다.

### P-004. 검증 불가능한 결과는 판매 가능한 결과로 표시하지 않는다

자동 검증을 통과하지 못한 결과는 `technical_status=rejected` 또는 `technical_status=needs_review`로 반환한다. `needs_review`는 완성된 판매 콘텐츠가 아니다.

### P-005. 생성 모델은 교체 가능해야 한다

제품 품질은 특정 회사나 특정 모델에 종속되지 않는다. 외부 API와 오픈소스 모델을 동일한 입력·출력 계약과 평가 하네스로 비교할 수 있어야 한다.

## 4. 제품 가치 공식

판매 가능한 생성 문제는 다음 네 요소를 모두 만족해야 한다.

```text
Commercial Value
  = Learning Fidelity
  × Reasoning Novelty
  × Mathematical Validity
  × Explanation Trust
```

곱셈으로 표현한 이유는 한 요소라도 사실상 0이면 상품 가치도 0에 가까워지기 때문이다.

| 요소 | 의미 | 실패 예시 |
|---|---|---|
| Learning Fidelity | Main의 학습 목표와 연결됨 | 원문과 전혀 다른 단원의 문제 |
| Reasoning Novelty | 새로운 추론·조건 해석이 필요함 | 숫자와 기호만 치환 |
| Mathematical Validity | 조건, 정답, 풀이가 수학적으로 유효함 | 해가 없거나 복수 정답인데 단답 처리 |
| Explanation Trust | 해설이 논리적이고 재검산 가능함 | 정답은 맞지만 중간 풀이가 모순 |

## 5. 용어 정의

### 5.1 Main Question

변형의 출발점이 되는 원문 고난도 문제이다.

### 5.2 Sketch Question

Main 전체를 단순하게 다시 만든 문제가 아니다. Main 풀이에서 막히기 쉬운 일부 조건, 선수 개념 또는 핵심 병목을 분리하여 연습하게 하는 계단형 문제다.

- 기본적으로 Main보다 쉽다.
- Main의 어떤 병목을 연습하는지 명시해야 한다.
- 의미 있는 병목을 찾지 못하면 생성을 보류할 수 있다.

### 5.3 Addition Question

Main의 핵심 아이디어를 유지하면서 조건 구조, 표현, 보조 개념 또는 풀이 분기를 변경한 확장형 문제다.

- Main과 비슷하거나 조금 더 어려운 난이도를 목표로 한다.
- `Addition`은 관계 유형이고 난이도 그 자체가 아니다.
- 숫자·계수·함수명·보기만 바꾼 결과는 Addition으로 인정하지 않는다.

### 5.4 Problem Family

하나의 Main과 해당 Main에서 파생된 모든 Sketch 및 Addition을 묶은 단위다.

### 5.5 Problem Blueprint

문제의 표면 문장이 아니라 문제를 구성하는 학습·수학적 구조를 표현한 데이터다.

- 개념 및 선수 지식
- 주어진 조건
- 구해야 하는 목표
- 예상 풀이 단계
- 핵심 불변 요소
- 풀이 병목
- 표현 방식
- 난이도 요인

### 5.6 Variation Plan

생성 전에 작성하는 변형 설계다. 무엇을 보존하고 무엇을 변경하며 어떤 변경을 금지할지 정의한다.

### 5.7 Accepted Candidate

필수 자동 검증을 모두 통과하여 `technical_status=accepted`로 표시할 수 있는 생성 문제다. 이는 기술 검증 상태이며 판매 또는 공개 승인을 뜻하지 않는다.

### 5.8 Needs Review

일부 자동 검증은 통과했지만 수학적 유효성 또는 관계 품질을 충분히 확정하지 못한 결과다. 판매 가능한 결과로 간주하지 않는다.

### 5.9 Publication Status

기술 검증과 별도로 운영자가 판매·공개 가능 여부를 관리하는 상태다.

- `draft`: 아직 판매·공개 승인되지 않음
- `approved`: `technical_status=accepted`이며 운영자가 수학적·표현·권리 검토를 승인함
- `withheld`: 검증 실패, 권리 문제 또는 운영 판단으로 판매·공개하지 않음

새 생성 결과의 기본값은 `draft`다. 시스템은 기술 검증만으로 `approved`를 자동 설정하지 않는다.

## 6. 사용자와 핵심 사용 시나리오

### Primary User

수험생용 문제 콘텐츠를 제작하는 운영자 또는 문제 제작자다.

### Secondary User

생성된 문제와 해설을 학습 콘텐츠로 사용하는 수험생이다.

### UC-001. Sketch 생성

1. 운영자가 Main 문제를 입력한다.
2. 시스템이 Main의 Blueprint와 풀이 병목을 분석한다.
3. 시스템이 Sketch 생성 가능 여부를 판단한다.
4. 가능하면 병목을 연습하는 문제 후보를 생성한다.
5. 독립 풀이와 검증을 통과한 결과만 반환한다.

### UC-002. Addition 생성

1. 운영자가 Main 문제를 입력한다.
2. 시스템이 핵심 아이디어와 변경 가능한 요소를 분석한다.
3. 시스템이 구조적으로 다른 변형 계획을 만든다.
4. 복수 후보 문제, 정답과 해설을 생성한다.
5. 수학적 유효성, Main과의 관계와 변형성을 평가한다.
6. 가장 높은 품질의 통과 후보를 반환한다.

### UC-003. 생성 실패 처리

1. 후보가 정답 검증, 중복 검사 또는 변형성 검사에 실패한다.
2. 시스템이 제한된 횟수만큼 수정 또는 재생성한다.
3. 끝까지 통과하지 못하면 결과를 숨기고 실패 원인을 반환한다.

## 7. 목표와 비목표

### 7.1 MVP 목표

- 텍스트와 LaTeX 기반 Main 입력 지원
- 지원 영역의 Problem Blueprint 추출
- Sketch 또는 Addition 문제 생성
- 정답과 단계별 해설 생성
- 구조화 출력 검증
- 독립 재풀이 및 가능한 범위의 수학 검증
- 숫자 치환, 원문 복제와 기존 데이터 중복 탐지
- 생성 근거와 검증 결과 저장
- 모델 교체 가능한 생성 인터페이스
- 브라우저 기반 시연

### 7.2 MVP 비목표

- 기하, 평면벡터, 공간벡터 문제 생성
- 새로운 기하 도형 또는 그래프 자동 작도
- 이미지가 없으면 해석할 수 없는 문제 생성
- 모든 고등학교 수학 영역 지원
- 파인튜닝 선행
- 특정 모델 또는 클라우드 고정
- 비즈니스 결제 기능
- 인터뷰 및 외부 평가 일정

### 7.3 최후순위 확장

기하·벡터 문제와 작도 시스템은 MVP와 최종 발표 시스템이 안정화된 뒤에만 연구한다. 현재 핵심 아키텍처가 해당 기능을 막지는 않아야 하지만, 이를 위해 MVP 일정을 희생하지 않는다.

## 8. 지원 범위 정책

초기 지원 과목은 JSON 데이터 진단 후 확정한다. 다음 조건을 만족하는 문항군을 우선한다.

- 문제, 보기, 정답과 해설이 텍스트 또는 LaTeX로 완결됨
- 그림 없이 문제의 모든 조건을 이해할 수 있음
- SymPy 또는 수치 표본 검사로 일부 검증 가능함
- 동일하거나 인접한 주제의 문제 가족 예시가 충분함

지원하지 않는 입력은 억지로 처리하지 않고 `technical_status=unsupported`와 이유를 반환한다.

## 9. 입력 계약

### IN-001. 필수 입력

```json
{
  "problem_text": "string",
  "target_role": "sketch | addition"
}
```

### IN-002. 선택 입력

```json
{
  "problem_latex": "string | null",
  "choices": ["string"],
  "known_answer": "string | null",
  "known_solution": "string | null",
  "subject": "string | null",
  "topic": "string | null",
  "difficulty_target": "easier | similar | harder | null",
  "assets": []
}
```

### IN-003. 입력 거절 조건

- 문제 본문이 없음
- 지원하지 않는 이미지 의존 문제
- 수식이 심각하게 깨져 의미를 복원할 수 없음
- 목표 역할이 정의되지 않음
- Main 자체가 해석 또는 풀이 불가능함

## 10. 출력 계약

### OUT-001. 기술 검증을 통과한 생성 실행 결과

```json
{
  "run_id": "string",
  "technical_status": "accepted",
  "publication_status": "draft",
  "source_blueprint": {},
  "variation_plan": {},
  "candidate": {
    "role": "sketch | addition",
    "problem_text": "string",
    "problem_latex": "string | null",
    "answer_type": "multiple_choice | short_answer | expression",
    "choices": [],
    "correct_answer": "string",
    "solution_blocks": []
  },
  "verification": {
    "passed": true,
    "checks": [
      {
        "gate_id": "QG-001",
        "status": "pass",
        "evidence": "string",
        "verifier_version": "string"
      }
    ]
  },
  "failure_reasons": [],
  "provenance": {}
}
```

### OUT-002. 실패 또는 지원 불가 결과

```json
{
  "run_id": "string",
  "technical_status": "rejected",
  "publication_status": "withheld",
  "source_blueprint": null,
  "variation_plan": null,
  "candidate": null,
  "verification": {
    "passed": false,
    "checks": []
  },
  "failure_reasons": [
    {
      "code": "ANSWER_MISMATCH",
      "message": "생성 답과 독립 풀이 결과가 일치하지 않음",
      "gate_id": "QG-003"
    }
  ],
  "provenance": {}
}
```

`rejected`와 `unsupported`에서는 `candidate`가 `null`일 수 있다. `needs_review`에서는 검토할 후보를 포함할 수 있지만 판매·공개 가능한 결과로 표시하지 않는다. 모든 실패 결과는 `failure_reasons`를 하나 이상 포함한다.

### OUT-003. Variation Plan 최소 필드

```json
{
  "target_role": "addition",
  "preserved_elements": ["string"],
  "changed_elements": [
    {
      "type": "constraint_structure | representation | auxiliary_concept | solution_branching | target_quantity",
      "description": "string"
    }
  ],
  "forbidden_changes": ["numeric_substitution_only", "surface_paraphrase_only"],
  "expected_solution_outline": ["string"],
  "difficulty_rationale": "string"
}
```

### OUT-004. Provenance 최소 필드

- 생성 모델 식별자
- 프롬프트 버전
- 데이터 또는 검색 인덱스 버전
- 생성 시각
- 샘플링 설정
- 재시도 횟수
- 검증기 버전

### OUT-005. 복수 후보 기록

각 후보 시도는 후보 순번, 생성 결과, 독립 풀이, 게이트별 판정, 선택용 게이트 점수와 실패 이유를 보존한다. 게이트 점수는 실패 후보를 제거하고 하나를 선택하기 위한 결정적 기준선일 뿐 교육적 상품성 점수가 아니다. 최종 후보도 미평가 Hard Gate가 있으면 `needs_review`를 유지한다.

## 11. 기능 요구사항

### 데이터 및 스키마

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-001 | 시스템은 원본 JSON을 변경하지 않고 읽어야 한다. | Must |
| FR-002 | 시스템은 모든 입력 레코드를 canonical schema로 검증해야 한다. | Must |
| FR-003 | 시스템은 Main·Sketch·Addition을 `family_id`로 연결해야 한다. | Must |
| FR-004 | 시스템은 누락, 깨진 수식, 잘못된 메타데이터에 quality flag를 부여해야 한다. | Must |
| FR-005 | 학습·평가 데이터는 문제 가족 단위로 분할해야 한다. | Must |
| FR-006 | 시스템은 `(시험, 과목, 번호)` 등 원본 복합키에서 안정적인 `source_problem_id`를 생성해야 한다. | Must |
| FR-007 | 시스템은 과목명과 표시용 소단원명의 이형 표기를 정규화하되 원본 값을 함께 보존해야 한다. | Must |
| FR-008 | 소단원 분류 조인은 표시 문자열이 아닌 `소단원_id`를 기준으로 해야 한다. | Must |
| FR-009 | `statement`에 포함된 문항번호, 배점, 보기와 그림 설명을 구조화 필드로 분리하되 원문도 보존해야 한다. | Must |
| FR-010 | 객관식 기호, 원시 `answer_value`와 정규화 수학 답을 분리해 저장해야 한다. | Must |
| FR-011 | HWP 수식 스크립트 원문과 향후 LaTeX·AST 정규화 결과를 별도 필드로 보존해야 한다. | Must |
| FR-012 | 렌더 이미지 존재 여부와 문제 풀이에 그림이 필요한지를 별도 품질 정보로 관리해야 한다. | Must |
| FR-013 | donor 문항에 존재하지 않는 문제 가족 역할을 추정하여 채우면 안 된다. | Must |
| FR-014 | 의미상 중복 문항은 `duplicate_group_id`로 연결하고 같은 평가 분할에 배치해야 한다. | Must |
| FR-015 | 원시 객관식 기호의 유니코드 변형을 선택지 번호로 정규화하되 원문을 보존해야 한다. | Must |
| FR-016 | PDF의 명시적 섹션 배치로 복원한 가족 관계는 근거, 신뢰도와 원문 페이지 위치를 함께 저장해야 한다. | Must |
| FR-017 | `family_role`은 문항의 실제 출처·저자 표기와 분리해 저장해야 한다. | Must |

### 분석 및 계획

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-101 | 시스템은 Main에서 Problem Blueprint를 구조화해 추출해야 한다. | Must |
| FR-102 | Blueprint에는 개념, 조건, 목표, 풀이 단계, 불변 요소, 병목과 난이도 요인이 포함되어야 한다. | Must |
| FR-103 | 시스템은 Sketch 생성 전에 의미 있는 병목 존재 여부를 판단해야 한다. | Must |
| FR-104 | 시스템은 생성 전에 Variation Plan을 작성해야 한다. | Must |
| FR-105 | Variation Plan은 보존 요소, 변경 요소, 금지 변형과 예상 풀이를 포함해야 한다. | Must |

### 검색 및 문맥 구성

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-201 | 시스템은 과목·주제·Blueprint를 기준으로 관련 문제 가족을 검색해야 한다. | Should |
| FR-202 | 검색 결과와 검색 점수는 생성 실행에 기록되어야 한다. | Must |
| FR-203 | 평가 대상 가족의 정답 또는 파생 문항이 생성 문맥에 누출되지 않아야 한다. | Must |
| FR-204 | 평가 가족과 정확·근사 중복인 donor 문항은 해당 평가 실행의 검색 후보에서 제외해야 한다. | Must |

### 생성

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-301 | 시스템은 Variation Plan을 입력으로 복수 후보를 생성할 수 있어야 한다. | Must |
| FR-302 | 각 후보는 문제, 답, 해설과 예상 풀이 구조를 포함해야 한다. | Must |
| FR-303 | 생성 결과는 Pydantic 스키마 검증을 통과해야 한다. | Must |
| FR-304 | 특정 LLM SDK 호출은 `LLMProvider` 경계 밖으로 노출되지 않아야 한다. | Must |
| FR-305 | 동일한 평가 하네스로 외부 API와 로컬 모델을 실행할 수 있어야 한다. | Must |

### 검증 및 선택

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-401 | 생성기와 분리된 독립 풀이 단계가 있어야 한다. | Must |
| FR-402 | 생성 답과 독립 풀이 답의 일치 여부를 검사해야 한다. | Must |
| FR-403 | 지원 가능한 수식은 SymPy 또는 수치 검사로 추가 검증해야 한다. | Must |
| FR-404 | 기존 데이터 및 Main과의 정확·근사 중복을 검사해야 한다. | Must |
| FR-405 | 숫자·계수·기호만 바뀐 변형을 탐지해야 한다. | Must |
| FR-406 | Main과의 학습 관계를 근거와 함께 평가해야 한다. | Must |
| FR-407 | 검증 실패 결과는 `technical_status=accepted`로 반환하면 안 된다. | Must |
| FR-408 | 제한된 재시도 후에도 실패하면 거절 이유를 반환해야 한다. | Must |
| FR-409 | 통과 후보가 여러 개이면 품질 점수로 최종 후보를 선택해야 한다. | Should |
| FR-410 | 각 검증 게이트는 통과·실패 값뿐 아니라 판정 근거, 검증기 버전과 실패 코드를 저장해야 한다. | Must |
| FR-411 | 시스템은 `technical_status`와 `publication_status`를 분리해 저장해야 한다. | Must |
| FR-412 | 시스템은 기술 검증만으로 `publication_status=approved`를 자동 설정하면 안 된다. | Must |
| FR-413 | 실패 결과는 하나 이상의 구조화된 `failure_reasons`를 포함해야 한다. | Must |

### API·UI·기록

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-501 | 운영자는 브라우저에서 Main과 목표 유형을 입력할 수 있어야 한다. | Must |
| FR-502 | UI는 Blueprint와 Variation Plan을 확인할 수 있어야 한다. | Should |
| FR-503 | UI는 문제, 정답, 해설과 검증 상태를 구분해 표시해야 한다. | Must |
| FR-504 | 모든 실행은 고유한 `run_id`를 가져야 한다. | Must |
| FR-505 | 실행 입력, 출력, 모델, 프롬프트와 검증 결과가 저장되어야 한다. | Must |
| FR-506 | 사용자는 결과를 JSON 또는 복사 가능한 형식으로 내보낼 수 있어야 한다. | Should |

## 12. 품질 요구사항

### 12.1 Hard Gates

다음 항목 중 하나라도 실패하면 후보를 `technical_status=accepted`로 표시할 수 없다. 모든 Hard Gate를 통과해도 판매·공개 승인은 별도 절차로 결정한다.

| ID | 필수 게이트 |
|---|---|
| QG-001 | 출력 스키마의 필수 필드가 모두 존재한다. |
| QG-002 | 문제 조건에서 답이 존재하고 목표 답 형식과 일치한다. |
| QG-003 | 생성 답과 독립 풀이 결과가 일치한다. |
| QG-004 | 해설의 최종 결론이 저장된 정답과 일치한다. |
| QG-005 | Main 또는 데이터셋의 기존 문항과 정확히 일치하지 않는다. |
| QG-006 | 숫자·계수·기호·고유명사만 변경한 변형이 아니다. |
| QG-007 | `preserved_elements`가 하나 이상 존재한다. |
| QG-008 | 의미 있는 `changed_elements`가 하나 이상 존재한다. |
| QG-009 | Addition은 변경 요소 중 하나 이상이 조건 구조, 표현, 보조 개념, 풀이 분기 또는 목표량에 해당한다. |
| QG-010 | Sketch는 Main의 특정 병목과 직접 연결된다. |
| QG-011 | 지원하지 않는 도형·이미지 정보에 암묵적으로 의존하지 않는다. |
| QG-012 | 검증 근거와 provenance가 저장된다. |
| QG-013 | `publication_status=approved`는 `technical_status=accepted`이며 운영자 승인이 기록된 경우에만 가능하다. |

### 12.2 Ranking Criteria

Hard Gate를 통과한 후보 사이에서 다음 항목으로 우선순위를 결정한다.

- Main 학습 목표 보존 정도
- 풀이 구조의 새로움
- 조건의 자연스러움
- 난이도 목표 적합성
- 해설의 명확성과 완결성
- 기존 데이터와의 거리
- 불필요한 계산량 또는 억지 설정의 적음

### 12.3 초기 정량 기준

데이터 진단 전 임계값을 임의로 고정하지 않는다. 다만 다음 기준은 처음부터 고정한다.

- `technical_status=accepted` 결과의 스키마 통과율: 100%
- `technical_status=accepted` 결과의 생성 답·독립 풀이 답 일치율: 100%
- 정확 중복으로 판정된 결과의 허용률: 0%
- 숫자 치환 전용으로 판정된 결과의 허용률: 0%
- 검증 기록이 없는 `technical_status=accepted` 결과의 허용률: 0%
- 같은 문제 가족의 train/test 동시 포함 허용률: 0%

유사도 임계값, 자동 승인 비율, 지연시간 목표와 과목별 통과 목표는 JSON 분석과 기준선 실행 후 이 문서에 추가한다.

## 13. 숫자 치환 및 변형성 판정

하나의 점수나 LLM 판정만으로 숫자 치환을 결정하지 않는다.

### 13.1 검사 신호

- 숫자와 기호를 마스킹한 문장 구조 유사도
- 수식 AST 또는 정규화 수식 구조 유사도
- 조건의 개수와 관계 구조 일치
- 목표량과 답 형식의 동일성
- 예상 풀이 단계의 동일성
- Variation Plan의 변경 요소
- 데이터베이스 최근접 문제와의 거리

### 13.2 기본 판정 규칙

다음 조건을 모두 만족하면 숫자 치환 의심 후보로 본다.

1. 숫자와 기호를 마스킹한 뒤 문제 구조가 거의 동일하다.
2. 수식 구조와 풀이 단계가 동일하다.
3. 의미 있는 변경 요소를 증명하지 못한다.

의심 후보는 자동 승인하지 않고 재생성하거나 `technical_status=needs_review`로 분류한다.

## 14. 평가 설계

### 14.0 하네스 구축 방식

평가 하네스는 생성 기능을 모두 만든 다음 한 번에 완성하는 별도 단계가 아니다. 데이터 로더가 준비되면 다음의 **최소 하네스**를 먼저 만든 뒤, 각 기능을 구현할 때 해당 기능의 검사를 같은 하네스에 추가한다.

```text
최소 하네스
  입력 고정 + 실행 설정 기록 + 출력 스키마 검사 + 결과 저장
        ↓
단순 기준선 생성기 연결
  생성 성공률 + 실패 로그 + 비용·지연시간 측정
        ↓
Blueprint 분석기 연결
  분석 스키마 + 핵심 개념·병목 보존 검사 추가
        ↓
Variation Planner와 RAG 연결
  변경 근거 + 검색 누수 + 출처·유사도 검사 추가
        ↓
생성·수학 검증기 연결
  정답·해설 일치 + 풀이 가능성 + 숫자 치환 탐지 추가
```

따라서 "하네스 구축 완료 후 Blueprint와 RAG를 시작한다"는 말은 **최종 평가기가 완성될 때까지 기다린다**는 뜻이 아니다. 동일 입력을 반복 실행하고 결과를 비교할 수 있는 최소 뼈대를 먼저 고정한다는 뜻이다. Blueprint, RAG와 검증기가 추가될 때마다 하네스도 함께 확장하며, 모든 버전은 같은 고정 평가 세트에서 이전 기준선과 비교한다.

현재 최소 하네스에서 출력 스키마만 통과한 후보는 `technical_status=needs_review`다. 구현하지 않은 Hard Gate는 `not_evaluated_gates`에 기록하며 통과로 간주하지 않는다. 독립 풀이와 나머지 Hard Gate가 구현되기 전에는 `accepted`를 반환하지 않는다.

### 14.1 분할 원칙

```text
Main + 모든 Sketch + 모든 Addition = 하나의 평가 분할 단위
```

같은 가족의 문항을 학습과 평가에 나누어 넣지 않는다.

### 14.2 기존 자료의 역할

확보한 Main·Sketch·Addition 관계는 제작자가 작성한 positive reference로 사용한다. 다만 하나의 Main에 가능한 정답 변형이 하나뿐이라는 의미는 아니므로 생성 문장과 정답을 gold text로 정확 일치시키지 않는다.

### 14.3 평가 축

| 평가 축 | 주요 증거 |
|---|---|
| 구조 유효성 | Pydantic 검증 결과 |
| 수학적 유효성 | 독립 풀이, SymPy, 수치 검사 |
| 학습 목표 보존 | Blueprint와 관계 판정 |
| 풀이 새로움 | 풀이 단계 및 조건 구조 비교 |
| 중복 방지 | exact/near duplicate 검색 |
| 난이도 근사 | 단계 수, 개념 수, 분기 수, 계산 복잡도 |
| 해설 신뢰도 | 중간 결과와 최종 답 일치 |
| 운영성 | 지연시간, 재시도 횟수, 실패율, 비용 |

LLM judge는 관계나 자연스러움 평가를 도울 수 있지만 유일한 검증기로 사용할 수 없다.

## 15. 상태 모델

```text
technical_status
received
  → normalized
  → analyzed
  → planned
  → generated
  → verifying
      ├── accepted
      ├── rejected
      ├── needs_review
      └── unsupported

publication_status
draft → approved
      └→ withheld
```

각 상태 전환은 시간, 입력 버전, 담당 컴포넌트와 실패 이유를 기록한다. `publication_status=approved` 전환에는 승인 주체와 승인 시각을 추가로 기록한다.

## 16. 시스템 구성요소

| 구성요소 | 책임 |
|---|---|
| Ingestion | JSON 원본 읽기와 정규화 |
| Domain Models | Pydantic 기반 입력·출력 계약 |
| Analyzer | Main에서 Problem Blueprint 추출 |
| Retriever | 관련 문제 가족 검색 |
| Planner | Sketch·Addition Variation Plan 생성 |
| Generator | 복수 문제·정답·해설 후보 생성 |
| Solver | 생성기와 독립된 풀이 생성 |
| Math Verifier | 수식·수치·정답 검사 |
| Novelty Verifier | 중복·숫자 치환·풀이 구조 유사도 검사 |
| Ranker | 통과 후보 점수화 및 선택 |
| Run Store | 모든 실행과 provenance 저장 |
| API | 외부 호출 인터페이스 |
| UI | 입력, 결과와 검증 근거 표시 |

## 17. API 초안

| Method | Path | 목적 |
|---|---|---|
| POST | `/analyze` | Main의 Blueprint 생성 |
| POST | `/generate` | Sketch 또는 Addition 생성 실행 |
| POST | `/verify` | 기존 후보를 검증 |
| GET | `/runs/{run_id}` | 실행 결과와 로그 조회 |
| GET | `/health` | 서비스 상태 확인 |

API 상세 필드는 canonical schema 확정 후 별도 OpenAPI 스키마로 생성한다.

## 18. 기술 제약

- 주 개발 언어는 Python 3.11 이상이다.
- 데이터 모델과 LLM 구조화 출력은 Pydantic으로 검증한다.
- API는 FastAPI를 기본으로 한다.
- MVP UI는 Gradio를 기본으로 한다.
- 수학 검증은 가능한 범위에서 SymPy와 NumPy를 사용한다.
- 원본과 정규화 데이터는 분리한다.
- 생성 모델은 `LLMProvider` 인터페이스 뒤에 둔다.
- 초기 검색은 로컬 BM25/TF-IDF와 임베딩 코사인 검색으로 구현한다.
- 초기 규모에서는 별도 벡터 데이터베이스를 필수로 하지 않는다.
- 최종 모델은 평가 하네스 완성 후 결정한다.

## 19. 데이터 및 보안 요구사항

- 원본 PDF와 JSON은 수정하지 않는다.
- 배포 또는 재판매 권리가 확인되지 않은 원본 자료는 공개 저장소에 올리지 않는다.
- `data/raw/`, PDF, 모델 가중치와 민감한 실행 로그는 Git에서 제외한다.
- API 키와 자격 증명은 `.env` 또는 비밀 저장소에서 관리한다.
- 프롬프트와 로그에 API 키, 사용자 로컬 경로 또는 불필요한 원문 전체를 기록하지 않는다.
- 공개 또는 판매 전에 사용 데이터와 생성 결과의 권리 범위를 별도로 확인한다.

## 20. 장애 및 실패 처리

- JSON 파싱 실패: 레코드를 건너뛰지 말고 실패 목록과 이유를 기록한다.
- 구조화 출력 실패: 제한 횟수 내에서 형식 수정 요청 후 거절한다.
- 독립 풀이 불일치: 수정 또는 재생성하며 자동 승인하지 않는다.
- SymPy 판정 불능: 불합격으로 단정하지 않고 다른 검증으로 전환하거나 `technical_status=needs_review`로 처리한다.
- 검색 결과 없음: RAG 없이 생성할지 지원 불가로 처리할지 정책을 기록한다.
- 최대 재시도 초과: `technical_status=rejected`와 마지막 실패 이유를 반환한다.
- 모델 장애: provider 오류를 표준 오류 형식으로 변환한다.

## 21. 개발 게이트

### Gate A. Data Ready

- JSON 진단 리포트 완료
- canonical schema 확정
- 문제 가족 연결 및 quality flag 생성
- family 단위 평가 분할 완료

### Gate B. Baseline Ready

- 단일 프롬프트 생성 전 과정 동작
- 모든 실행 기록
- 기준선 평가 리포트 생성

### Gate C. Controlled Generation Ready

- Blueprint와 Variation Plan 생성
- 보존·변경 요소 추적
- Sketch·Addition 후보 생성

### Gate D. Verification Ready

- 독립 풀이 및 답 비교
- 수학 검증
- 중복 및 숫자 치환 검사
- `technical_status` 결정

### Gate E. Demo Ready

- API와 UI 통합
- 검증 상태 및 실패 이유 표시
- 재현 가능한 데모 시나리오

### Gate F. Model & Deployment Ready

- 외부 API와 오픈소스 후보 동일 평가
- 품질·속도·비용 비교
- 최종 모델과 배포 방식 결정

기하·벡터 확장은 Gate F 이후 별도 연구 게이트로 관리한다.

## 22. 현재 데이터 사실

- 문제·해설 PDF 3개 회차를 분석했다.
- Main 중심 문제 가족 41개를 확인했다.
- Sketch 19문항, Addition 145문항으로 총 205개 관련 문항을 확인했다.
- Sketch는 모든 Main에 존재하지 않는다.
- Addition은 난이도 등급이 아니라 아이디어 확장 관계다.
- PDF의 파일명, 회차명, 목차 및 제목에는 일부 불일치와 템플릿 잔재가 있다.
- PDF 수식 텍스트는 전용 글리프로 인해 깨질 수 있다.
- 현재 확보한 PDF와 donor pool을 전체 입력으로 보고 MVP 개발을 진행한다.

### 22.1 2026-09-03 Aleph 샘플 진단

- `0_읽어보기.md`, 소단원 체계 JSON, 기출 DB JSON, 생성 산출물 YAML을 확인했다.
- 설명서에 따르면 기존 기출 DB는 17개 회차 459문항이며, 이번에 제공된 파일은 구조 확인용 일부 샘플이다.
- 소단원 체계에는 41개 고유 `소단원_id`가 있다: 수학Ⅰ 9, 수학Ⅱ 9, 미적분 7, 확률과 통계 8, 기하 8.
- 기본 세트는 17문항, 48점이며 17개의 서로 다른 소단원 ID를 사용한다.
- 소단원 체계의 `meta.과목`에는 수학Ⅰ·수학Ⅱ·미적분만 적혀 있지만 실제 pool에는 확률과 통계와 기하도 존재한다.
- 기출 DB 샘플은 27문항이다: 공통 12, 미적분 5, 확통 5, 기하 5.
- 기출 DB 샘플은 객관식 23문항, 단답형 4문항이며 2점 5문항, 3점 22문항이다.
- 27문항 모두 필수 필드와 유효한 `소단원_id`를 가지며, 객관식의 선택지와 `answer_value`도 일치한다.
- 기출 DB에는 별도의 안정적인 `problem_id`, 원문 해설, Blueprint, Main·Sketch·Addition 관계가 없다.
- 수식은 Markdown 내부 LaTeX `$...$` 또는 `$$...$$` 형태다.
- 4문항은 `[그림: ...]` 설명을 포함하지만 실제 이미지 asset 필드는 없다.
- `statement` 안에 문항번호·배점·보기가 포함되며 `choices` 필드에도 보기가 중복 저장된다.
- 같은 `소단원_id`라도 표시용 `소단원` 문자열 형식이 체계 JSON과 DB에서 다르므로 ID를 조인 키로 사용해야 한다.
- 과목명은 `확률과통계`, `확통`, `확률과 통계`처럼 파일별로 다를 수 있다.
- 생성 산출물은 JSON이 아니라 YAML이며 `meta`와 `item`으로 구성된 3개 예시를 포함한다.
- 생성 산출물은 `remix_base`로 원문을 추적하고 `render → local → answer` 순서의 3단 게이트를 기록한다.
- 세 생성 예시의 저장된 답과 풀이 결과는 일치한다.
- `remix_base`는 사람이 읽는 문자열이므로 단독 외래키로 사용하지 않고 안정적인 `source_problem_id`를 함께 저장해야 한다.
- 생성 예시에는 보존 요소, 변경 요소, 풀이 구조 차이와 상세 검증 근거가 없다.
- 현재 3단 게이트는 유용한 출발점이지만, 우리 제품 기준의 새로운 풀이 경험을 검증하기에는 부족하다.
- 특히 일부 저난도 remix는 소재와 숫자만 바꾼 결과가 될 수 있으므로 고난도 Main 변형의 gold reference로 사용하지 않는다.
- 이 샘플은 2·3점 생성 파이프라인 구조를 보여주며, 고난도 4점 Main→Sketch/Addition 학습 데이터는 아니다.

### 22.2 2026-09-04 donor pool 진단

#### 구조와 완전성

- 파일명은 500이지만 ID 목록과 문항 파일은 각각 499개이며 ID 집합과 순서가 모두 일치한다.
- 499개 ID, 원문 HWPX 해시와 정규화 문장 해시는 각각 모두 고유하다.
- 모든 문항에 문제 본문, 정답, 해설과 수식 객체가 존재한다.
- 과목은 `MATH1` 160문항, `MATH2` 142문항, `CALCULUS` 197문항이다.
- Main·Sketch·Addition, `family_id` 또는 `remix_base` 관계 필드는 없다.

#### 품질과 사용 범위

- 수식은 LaTeX가 아니라 `[[EQ:...]]` 안의 HWP 수식 스크립트이며 21,421개 수식 객체와 마커 수가 모두 일치한다.
- 품질 경고가 있는 레코드는 7개이며, 본문·해설 이미지 누락 6건씩과 해설에서 정답을 복구한 1건이다.
- 본문 그림 객체를 포함한 문항은 104개다. 그림 키워드까지 보수적으로 제외한 깨끗한 텍스트 완결 후보는 368개다.
- 객관식은 유니코드 기호 변형 1건을 포함해 322문항이고 정수형 단답은 177문항이다.
- HWP 표기 차이를 제거한 유사도 0.95 이상 10쌍은 수동 확인 결과 의미상 같은 문항이므로 중복 그룹 처리가 필요하다.

상세 집계는 `docs/DATA_DIAGNOSTIC_500.md`, 스키마 제안은 `docs/CANONICAL_SCHEMA_DRAFT.md`에 기록한다. 문제 ID가 포함된 상세 결과는 Git에서 제외되는 `artifacts/private/`에 저장한다.

### 22.3 2026-09-04 PDF 문제 가족 manifest

- 본문 목차, 섹션 배치와 해설의 역할 표기를 대조해 41개 가족·205문항을 구조화했다.
- 회차별로 3회 71문항, 4회 66문항, 5회 68문항이다.
- 역할별로 Main 41, Sketch 19, Addition 145이며 Sketch가 있는 가족은 15개다.
- MVP 후보인 공통·미적분은 23개 가족, Main 23, Sketch 19, Addition 74로 총 116문항이다.
- `family_id`는 인쇄 번호 충돌을 피하기 위해 `(연도, 회차, 과목 영역, 인쇄 번호)`로 만든다.
- 일부 Sketch·Addition에는 기존 시험 출처가 표시돼 있으므로 가족 역할을 저자 생성 이력으로 해석하지 않는다.
- 5회에는 임시 주제명과 해설 내부 회차 표기 불일치가 있어 quality flag를 부여했다.
- PDF 관계 자료는 positive relation reference이며 생성 문장의 유일한 정답이나 직접 변형 계보가 아니다.
- 구조화 결과는 `data/interim/pdf_family_manifest.json`, 분석 근거는 `docs/PDF_FAMILY_ANALYSIS.md`에 기록한다.

### 22.4 구현된 정규화·분할 결과

- `canonical-problem-v0.2` Pydantic 모델로 donor 499문항과 PDF 205문항을 전부 검증했다.
- donor에는 가족 역할을 만들지 않았고, PDF 파생 문항에는 같은 가족의 Main ID를 연결했다.
- donor 내부 유사도 0.95 이상인 10개 그룹·20문항에 안정적인 `duplicate_group_id`를 부여했다.
- 공통·미적분 23개 가족은 3회 development 8개, 5회 validation 8개, 4회 test 7개로 분할했다. 확률과 통계·기하 18개 가족은 현재 범위에서 parked 처리했다.
- validation·test Main으로 Sketch/Addition 생성 과제 25개를 만들었다.
- PDF와 donor 간 한국어 3-gram Jaccard 0.4 이상을 누수 의심 후보로 보수적으로 표시했다. 전체 후보 14건 중 평가 가족 후보는 9건이고, 해당 donor 고유 ID 8개를 평가 검색에서 제외한다. 0.7 이상 4건만 높은 신뢰 후보이며 나머지는 증명이 아닌 검토 보조 신호다.
- PDF 해설은 2단 편집으로 텍스트 순서가 섞이므로 현재 정답과 해설 페이지까지만 연결하고 해설 본문은 `SOLUTION_TEXT_NOT_SEGMENTED`로 남겼다.

### 22.5 Blueprint·Variation Plan·검색 기준선

- `ProblemBlueprint`와 `VariationPlan`의 Pydantic 계약, 프롬프트 v1, 구조화 응답 파서를 구현했다.
- 실제 Blueprint와 계획 생성은 모델 연결 전이므로 아직 실행·평가하지 않았다.
- Python 표준 라이브러리 기반 BM25가 품질 경고와 그림 의존성이 없는 donor 368문항을 색인한다.
- 공통 Main에는 `MATH1`·`MATH2`, 미적분 Main에는 `CALCULUS` donor만 검색한다.
- 중복 그룹에서는 한 문항만 반환하고 평가 교차 중복 donor 8개를 강제로 제외한다.
- 고정 평가 과제 25개에서 각 5개 결과를 얻었고 제외 목록 누출은 0건이었다. 이는 검색 품질이나 교육적 관련성의 합격을 의미하지 않는다.

### 22.6 모델 독립 생성·검증 실행

- `analyze → retrieve → plan → generate → solve independently → verify → select` 전체 파이프라인을 구현했다.
- 요청당 1~5개 후보를 생성하고 각 후보를 별도로 재풀이한 뒤, 실패 게이트가 없는 후보를 우선 선택한다.
- 정답 형식, 생성 답·독립 풀이 답, Main·donor 완전 복제, 숫자 치환 구조와 지원하지 않는 이미지 지시어를 결정적으로 검사한다.
- 해 존재성·유일성과 자유 형식 해설의 모든 단계는 아직 결정적으로 검증하지 못하므로 `not_evaluated`로 기록한다. 따라서 실패가 없어도 `needs_review`이며 자동 `accepted`는 없다.
- 저장 응답 fixture를 사용한 전체 파이프라인 재현과 로컬 브라우저 GET 응답을 확인했다.
- 실제 모델 연결 후 고정 평가 과제 25개를 일괄 실행하고 문항별 결과와 전체 게이트 집계를 저장하는 실행기를 구현했다.
- OpenAI 호환 chat-completions 어댑터를 구현해 외부 API와 로컬 vLLM을 같은 계약으로 연결한다.
- localhost가 아닌 endpoint는 원문 데이터 외부 전송을 명시적으로 허용하지 않으면 생성자 단계에서 거절한다.
- Python 표준 라이브러리와 기존 Pydantic만 사용했으며, 외부 PyPI 연결 없이 editable 설치와 테스트가 가능하다.

## 23. 결정된 사항

| ID | 결정 | 상태 |
|---|---|---|
| DEC-001 | 구매자가 새로운 풀이 경험을 얻는 것을 최상위 가치로 둔다. | Fixed |
| DEC-002 | 숫자만 바꾼 문제는 생성 실패로 본다. | Fixed |
| DEC-003 | Sketch·Addition 관계와 난이도를 분리한다. | Fixed |
| DEC-004 | JSON을 주 데이터로, PDF를 구조·시각 검증 자료로 사용한다. | Fixed |
| DEC-005 | 최종 생성 모델은 하네스 완성 후 결정한다. | Fixed |
| DEC-006 | 모델 공급자 독립 구조를 사용한다. | Fixed |
| DEC-007 | 인터뷰는 개발 로드맵에서 제외한다. | Fixed |
| DEC-008 | 기하·벡터·자동 작도는 최후순위다. | Fixed |
| DEC-009 | 문제 가족 단위로 학습·평가 데이터를 분할한다. | Fixed |
| DEC-010 | 기술 검증 상태와 판매·공개 승인 상태를 분리하며, 검증 실패 결과를 판매 가능한 결과로 표시하지 않는다. | Fixed |
| DEC-011 | 소단원 분류의 조인 기준은 표시 문자열이 아니라 `소단원_id`다. | Fixed |
| DEC-012 | `remix_base` 외에 기계 판독 가능한 안정적 `source_problem_id`를 둔다. | Fixed |
| DEC-013 | 원시 문항·답 표현과 정규화된 문항·답 표현을 모두 보존한다. | Fixed |
| DEC-014 | 기존 `render/local/answer` 게이트를 재사용하되 구조적 새로움 검증을 추가한다. | Fixed |
| DEC-015 | 현재 확보한 499문항 donor와 PDF 41개 가족을 전체 입력으로 보고 추가 데이터 없이 MVP 개발을 진행한다. | Fixed |
| DEC-016 | PDF 가족은 명시된 교육적 관계 reference로 사용하되 유일한 gold 문장이나 저자 생성 계보로 간주하지 않는다. | Fixed |
| DEC-017 | `canonical-problem-v0.2`에서는 기존 donor `problem_id`를 안정적 ID로 보존하고 PDF 가족 ID는 연도·회차·과목·인쇄 번호·역할·순번으로 만든다. | Fixed |
| DEC-018 | MVP 공통·미적분 가족은 3회 development, 5회 validation, 4회 test로 고정하고 확률과 통계·기하는 parked 처리한다. | Fixed |
| DEC-019 | donor 내부 0.95 유사도는 중복 그룹화에, PDF-donor 간 0.4 유사도는 평가 검색 제외 후보에 사용한다. 두 임계값은 서로 다른 목적이며 기준선 결과로 재조정할 수 있다. | Provisional |
| DEC-020 | 최소 하네스에서 스키마만 통과한 결과는 `needs_review`이며 미구현 게이트를 명시한다. 모든 Hard Gate 전에는 자동 `accepted`가 없다. | Fixed |
| DEC-021 | PDF 해설 본문은 문항 경계를 신뢰할 수 있을 때까지 `null`로 두고 정답·페이지 근거만 사용한다. | Fixed |
| DEC-022 | 초기 RAG는 텍스트 완결 donor만 사용하는 로컬 BM25로 시작하고, 임베딩 검색은 기준선 대비 이득을 측정할 때만 추가한다. | Fixed |
| DEC-023 | 생성 파이프라인은 요청당 1~5개 후보를 지원하되 자동 게이트 점수는 실패 후보 제거·선택에만 사용하고 상품성 점수로 해석하지 않는다. | Fixed |
| DEC-024 | 결정적으로 검증하지 못한 Hard Gate가 하나라도 있으면 `verification.passed=false`, `technical_status=needs_review`로 둔다. | Fixed |
| DEC-025 | 외부 모델 endpoint로 원문을 보내는 동작은 명시적 `allow_remote` 승인 없이는 차단한다. localhost 모델은 별도 승인 없이 사용할 수 있다. | Fixed |
| DEC-026 | FastAPI·Gradio 도입 전 표준 라이브러리 HTTP 서버로 최소 브라우저 데모를 제공한다. | Fixed |

## 24. 미결 사항

다음 항목을 순차적으로 해결한다. 약 5,000문항 관련 질문은 추가 데이터가 실제로 들어올 때 다시 연다.

| ID | 질문 |
|---|---|
| OQ-001 | JSON의 최상위 구조와 필드명은 무엇인가? |
| OQ-002 | 수식은 LaTeX, MathML, 일반 텍스트 또는 이미지 중 무엇인가? |
| OQ-003 | 문제·보기·정답·해설의 누락률은 얼마인가? |
| OQ-004 | Main·Sketch·Addition 관계가 JSON에 존재하는가? |
| OQ-005 | 약 5,000문항 중 이미지 없이 완결되는 문항 비율은 얼마인가? |
| OQ-006 | 첫 MVP에서 가장 많은 가족과 가장 안정적인 검증이 가능한 과목은 무엇인가? |
| OQ-007 | 수식 정규화와 유사도 판정에 사용할 표현은 무엇인가? |
| OQ-008 | 숫자 치환 탐지 임계값은 얼마로 설정해야 하는가? |
| OQ-009 | 자동 승인 가능한 문항 유형은 어디까지인가? |
| OQ-010 | 판매 전 콘텐츠 검수와 권리 확인 절차는 어떻게 구성할 것인가? |
| OQ-011 | 전체 약 5,000문항도 donor pool과 같은 스키마와 수식 형식을 사용하는가? |
| OQ-012 | donor `problem_id`를 canonical ID로 그대로 사용할 것인가, 별도 내부 ID를 추가할 것인가? |
| OQ-013 | HWP 수식 스크립트를 LaTeX 또는 AST로 변환하는 시점과 허용 오류율은 얼마인가? |

OQ-004는 현재 자료 기준으로 donor JSON에는 관계 필드가 없고 PDF에는 명시 관계가 있는 것으로 확인했다. OQ-006의 MVP 범위는 공통·미적분 23개 가족으로 결정했다. OQ-012는 기존 donor ID 보존으로 해결했다. OQ-007·OQ-008은 텍스트 기준선만 구현된 부분 해결 상태이며 수식 AST와 생성 기준선 결과 후 다시 조정한다. 약 5,000문항 관련 OQ-001~OQ-005와 OQ-011은 추가 데이터가 실제로 들어올 때 다시 연다.

## 25. 추적성 규칙

- 구현 작업은 하나 이상의 `FR-*`, `QG-*` 또는 `DEC-*` ID와 연결한다.
- 테스트 이름 또는 테스트 설명에 대응 요구사항 ID를 남긴다.
- 요구사항이 없는 기능을 구현해야 한다면 먼저 이 문서를 갱신한다.
- 요구사항을 삭제하지 않고 변경 이력에 사유를 기록한다.
- README의 로드맵 완료 표시와 실제 구현 상태가 일치해야 한다.

## 26. 변경 이력

| 버전 | 날짜 | 변경 내용 |
|---|---|---|
| 0.7.1 | 2026-09-04 | 실제 모델의 25개 고정 평가 과제 일괄 실행·문항별 즉시 저장 도구와 실행 안내 추가 |
| 0.7.0 | 2026-09-04 | OpenAI 호환 provider, 전체 생성 파이프라인, 복수 후보·독립 풀이·결정적 검증·선택, 외부 데이터 전송 차단과 표준 라이브러리 로컬 데모 구현 |
| 0.6.0 | 2026-09-04 | canonical v0.2, donor/PDF 로더, 중복 그룹, 회차별 가족 분할, 25개 평가 과제, 교차 누수 제외 목록, 최소 모델 독립 하네스, Blueprint/Variation Plan 계약과 로컬 BM25 기준선 구현 결과 반영 |
| 0.5.0 | 2026-09-04 | PDF 3개 회차에서 41개 가족·205문항 manifest 확정. 추가 데이터 없이 MVP 진행, 가족 역할·출처 분리와 donor 교차 중복 차단 요구사항 반영 |
| 0.4.0 | 2026-09-04 | donor pool 499문항 진단 반영. HWP 수식 보존, 그림 의존성 분리, donor 역할, 중복 그룹과 정답 기호 정규화 요구사항 추가 |
| 0.3.0 | 2026-09-04 | `AGENTS.md` 실행 규칙 축약. 기술 검증과 판매 승인 상태 분리, 실패 응답 계약과 관련 요구사항 추가 |
| 0.2.0 | 2026-09-03 | Aleph 소단원·기출 DB·생성 YAML 샘플 진단 결과 반영. ID 정규화, 원문 추적성, 답 정규화와 상세 게이트 근거 요구사항 추가 |
| 0.1.0 | 2026-09-02 | 최초 작성. 상업적 학습 가치, SDD 요구사항, 품질 게이트, 모델 독립성과 기하·벡터 후순위 원칙 정의 |
